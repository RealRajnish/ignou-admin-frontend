import React from "react";
import styled from "styled-components";

const AdoptionPage = () => {
  return <Wrapper>Adoption Page</Wrapper>;
};

const Wrapper = styled.section``;

export default AdoptionPage;
